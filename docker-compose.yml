services:
  mongo:
    image: mongo:6-jammy
    command: mongod --wiredTigerCacheSizeGB 0.5
    restart: unless-stopped
    attach: false
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - default

  redis:
    image: redis:7.2-alpine
    restart: unless-stopped
    attach: false
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - default

  embedding-server:
    build:
      context: .
      dockerfile: Dockerfile.embedding
    image: distillnews-embedding:latest
    ports:
      - "8001:8001"
    environment:
      EMBEDDING_MODEL_NAME: sentence-transformers/all-MiniLM-L6-v2
    restart: unless-stopped
    networks:
      - default

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    image: distillnews-backend:latest
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      DB_URL: ${DB_URL:-mongodb://mongo:27017/evolution}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:3000}
      PORT: 8000
      EMAIL: ${EMAIL}
      PASS: ${PASS}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      EMBEDDING_SERVICE_URL: http://embedding-server:8001
    depends_on:
      mongo:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - gateway-network
      - default

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: distillnews-frontend:latest
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - gateway-network
      - default

networks:
  gateway-network:
    external: true

volumes:
  mongo_data:
